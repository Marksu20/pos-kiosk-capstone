SIGN IN
    email
    password
    forgot password

SIGN UP
    company name
    email
    password
    confirm password

POS
    Home {
        search
        categories(list)
        process order
    }
    Orders(from kiosk) {
        waiting(update, delete order)
        in process(update)
        to serve(done)
    }
    receipts(view)

ADMIN 
    Dashboard {
        filter
        total product should
        total customer
        total revenue
        best sellers data(bar graph)
        recent transactions
    }
    Products {
        add 
        edit
        delete 
        search(name, price)
        track quantity(optional)
    }
    Categories {
        add
        edit
        delete
        search(name)
    }
    Receipts {
        view
        print
        delete
        search(order no, customer)
    }
    Discounts {
        add
        edit
        delete
        search(name, value)
    }
    Account{
        filter users(cashier, sub-admin)
        create another user(cashier, sub-admin) {
             add
             edit
             delete
        }
        link/ qr code of kiosk
        account email and Company Name
    }

KIOSK
    best seller(landing page)
    categories
    select orders
    payment(cash/ paypal)